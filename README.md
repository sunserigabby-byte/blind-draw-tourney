
# Sunny Sports Performance — Blind Draw

A Vite + React + TypeScript + Tailwind single-page app for running reverse coed grass tournaments.

## Local dev
```bash
npm install
npm run dev
```

## Build
```bash
npm run build
npm run preview
```

## Deploy on Vercel
- Import the repo in Vercel
- Build Command: `npm run build`
- Output Directory: `dist`
- Framework preset: Vite

git add tsconfig.json package.json vite-env.d.ts
git commit -m "Fix TS include + decouple tsc from build"
git push
